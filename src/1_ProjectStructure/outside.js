import React from 'react';
import {
  StyleSheet
} from 'react-native';

export default outside = StyleSheet.create(
{
  viewStyle: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: '#FFF',
  },

  textStyle: {
    fontSize: 80,
    textAlign: "center",
    color: '#F88',
    fontSize: 20
  },
 
});

module.exports = outside;